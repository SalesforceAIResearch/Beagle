"""Example external benchmark plug-in package.

The leaf package keeps a regular ``__init__.py`` (not a namespace
package — that's only the ``xrlenv_plugins/`` and
``xrlenv_plugins/benchmarks/`` levels above). Module-level docstring
goes here for ``help(xrlenv_plugins.benchmarks.example_bench)`` and
Sphinx autodoc.
"""
