"""Spine smoke template: a Python+aiohttp container the in-sandbox stub
drives via :class:`xrlenv.templates.hello_shell.adapter.ShellEnvAdapter`.
Used by ``tests/smoke/single_rollout.py`` and the platform's own
integration tests; not a benchmark."""
